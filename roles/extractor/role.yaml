# Role manifest — Экстрактор (R2)
# Source-of-truth: DP.ROLE.001 §3.2

name: extractor
id: R2
type: agential
display_name: "Экстрактор"

runner: scripts/extractor.sh

install:
  auto: true
  priority: 2

notify_template: extractor.sh
